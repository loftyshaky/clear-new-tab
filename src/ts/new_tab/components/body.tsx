import React from 'react';
import { observer } from 'mobx-react';
import { c_background } from 'new_tab/internal';

export const Body: React.FunctionComponent = observer(() => <c_background.BackgrountContainer />);
